name: carvajal_test
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.0.7
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  intl: ^0.17.0
  http: ^0.13.3
  flutter_staggered_grid_view: ^0.4.0

  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/GlobalVars/Language/es.json
    - assets/
    - assets/logos/